[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cs5381-local-agent"
version = "0.1.0"
dependencies = [
  "numpy",
  "sentence-transformers",
  "faiss-cpu"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]